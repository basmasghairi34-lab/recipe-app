[build]
  publish = "frontend"

[[redirects]]
  from = "/api/*"
  to = "https://YOUR_BACKEND_URL.onrender.com/:splat"
  status = 200
  force = true

# This section is for optional Netlify Functions if they decide to go that route
[functions]
  directory = "backend/functions"
